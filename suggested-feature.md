
# Suggested Feature

```bash
Embedded bank system : 
why : to make user pay easier without opening the bank, because right now user need to open the bank apps instead of pay instantly
```

```bash
Quantity product available : 
why: to make user know how many item left, because right now as user don't know how much each item has
```

```bash
Buy now feature:
Why: to redirect user to checkout faster, because right now user always need to put it in the basket first
```